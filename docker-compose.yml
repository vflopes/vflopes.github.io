services:
  jekyll:
    build:
      context: .
      dockerfile: Dockerfile
    image: jekyll:local
    ports:
      - "4000:4000"
      - "35729:35729"
    volumes:
      - .:/srv/jekyll
    command: bundle exec jekyll serve --host 0.0.0.0 --livereload
